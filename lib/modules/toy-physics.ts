/**
 * M4 placeholder — Rapier-backed toy-world physics module.
 *
 * In M1–M3 the toy chips use the lightweight pointer-spring implementation
 * in Interactions.tsx. This module boundary exists now so the lazy-loading
 * contract (load one world ahead, evict two behind) is designed in from
 * the start.
 */
export const TOY_PHYSICS_MODULE = "m4-placeholder";
